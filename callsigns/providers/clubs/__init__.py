"""CW club providers.

Clubs publish scores rather than logs, so these providers yield entrants with
an activity measure — QSO counts, scores — rather than the far larger set of
callsigns those entrants worked. That still ranks activity, which is the point.
"""
